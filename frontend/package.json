{
  "name": "frontend",
  "version": "0.5.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.1",
    "@testing-library/react": "^12.1.2",
    "@testing-library/user-event": "^13.5.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "^5.0.0",
    "web-vitals": "^2.1.2"
  },
  "devDependencies": {
    "dree": "^3.3.4",
    "stylus": "^0.56.0"
  },
  "scripts": {
    "start": "node compileStylus.js && react-scripts start",
    "build": "node compileStylus.js && react-scripts build",
    "test": "node compileStylus.js && react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
